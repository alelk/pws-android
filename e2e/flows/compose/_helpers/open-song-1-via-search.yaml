appId: ${APP_ID}
name: "_helper: open song #1 via Search tab"
---
# Reusable subflow. Navigates to the Search tab, types the numeric query,
# waits for the first visible result (index-based tag), and taps it.
# Leaves the app on SongDetailScreen with ACTION_TOGGLE_FAVORITE visible.

- tapOn:
    id: "${NAV_SEARCH}"

- extendedWaitUntil:
    visible:
      id: "${FIELD_SEARCH}"
    timeout: ${TIMEOUT_MEDIUM}

- tapOn:
    id: "${FIELD_SEARCH}"

- eraseText
- inputText: "${SEARCH_QUERY_NUMERIC}"

- extendedWaitUntil:
    visible:
      id: "${SEARCH_SUGGESTION_FIRST}"
    timeout: ${TIMEOUT_LONG}

- tapOn:
    id: "${SEARCH_SUGGESTION_FIRST}"

- extendedWaitUntil:
    visible:
      id: "${ACTION_TOGGLE_FAVORITE}"
    timeout: ${TIMEOUT_LONG}
