<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alelk.pws.database">

    <application android:allowBackup="true" android:label="@string/app_name" android:supportsRtl="true">
        <provider
            android:authorities="@string/db_authority"
            android:name="com.alelk.pws.database.provider.PwsDataProvider"
            android:exported="true"
            android:syncable="true">
        </provider>
    </application>

</manifest>
